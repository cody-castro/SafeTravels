Name of Trip: <%= @trip.name %><br>
Destination City: <%= @trip.destination.city_name %><br>

<%=image_tag @trip.destination.img_url, width: 700%>

<%= link_to "Delete", trip_path(@trip), :confirm => "Are you sure?", :method => :delete %>